{
  "fixture": "trigger_cause_skill_load",
  "_note": "Skill activation triggered by content fetched from an untrusted URL. Primary signal is trigger_cause_violation. The fetched content also contains a comment-hidden system directive, so instruction_shape_in_tool_result fires (correctly), and the session graph shape resembles the triangle motif so spectral picks it up.",
  "expected_findings": [
    {"type": "trigger_cause_violation", "scope": "dynamic", "severity": "high"},
    {"type": "instruction_shape_in_tool_result", "scope": "dynamic", "severity": "medium"},
    {"type": "spectral_motif_match", "scope": "static", "severity": "medium"}
  ]
}
